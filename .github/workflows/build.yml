# name: 'Build Corne 3x6 nice!view nice!nano Firmware'
name: Test Corne 3x6 nice!view nice!nano
on:
  workflow_dispatch:
  push:
    paths:
      - 'config/west.yml'
      - 'config/shared.dtsi'

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
